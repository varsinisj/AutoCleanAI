services:
  - type: web
    name: autoclean-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
```

**5. Save it** → `Ctrl + S`

---

## Your folder should now look like this
```
AutoCleanAI/
├── app.py
├── clean_dataset.py
├── requirements.txt
├── render.yaml        ← new file you just created
├── .gitignore         ← create this too
├── README.md          ← create this too
├── agents/
│   ├── decision.py
│   ├── executor.py
│   ├── profiler.py
│   ├── validator.py
│   └── __init__.py
├── templates/
│   └── index.html
├── static/
└── uploads/           ← this folder won't go to GitHub (.gitignore)